header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours
    //- below line is equivalent to div.header__logo
    .header__logo
        img(src='/img/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        //- user is stored in the response.local as part of authController.isLoggedIn middleware
        if user
            a.nav__el.nav__el--logout Log out
            a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
                //- e.g. to display 'Sameer' if user name is 'Sameer Bhilare'
                span= user.name.split(' ')[0]
        else       
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='/signup') Sign up